{
  "name": "balowaci",
  "version": "1.0.0",
  "private": true,
  "description": "BaloWaci native app wrapper for the unified time interpretation system.",
  "scripts": {
    "app:prepare": "node scripts/prepare-capacitor.js",
    "serve": "node static-server.js",
    "cap:sync": "npm run app:prepare && npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0",
    "heic-convert": "^2.1.0"
  }
}
